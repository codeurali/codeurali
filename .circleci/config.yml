version: 2.1

jobs:
  checkout:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      
  test:
    docker:
      - image: cimg/base:2021.04
      steps:
        - checkout
        -run:
          name: "test du code"
          command: |
            ls -l 
            wc -l fichier.exe
            
workflows:
  checkout-test-workflow:
    jobs:
      - checkout
      - test
