version: 2.1
jobs:
  checkout:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      
  test:
    docker:
      - image: cimg/base:stable
      steps:
        -checkout
        -run:
          name: ""
          command: |
            echo "Vérifier la présence du code github"
            pwd
            ls -l 
            wc -l fichier.exe
            
workflows:
  checkout-test-workflow:
    jobs:
      - checkout
      - test
